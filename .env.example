# 로컬 개발 환경 설정 예시
# 이 파일을 복사해서 .env 파일을 만들고 실제 값으로 변경하세요

# === 로컬 개발 모드 (USE_SECRETS_MANAGER=false) ===
# 데이터베이스 설정 - 모든 정보를 환경 변수에서 가져옴
DB_USER=your_username
DB_PASS=your_password
DB_HOST=localhost
DB_PORT=3306
DB_NAME=museumdb

# AWS Secrets Manager 사용 여부 (로컬: false, EC2: true)
USE_SECRETS_MANAGER=false

# === EC2 프로덕션 모드 (USE_SECRETS_MANAGER=true) ===
# Secrets Manager에서 username, password만 가져옴
# Host, Port, DBName은 환경 변수로 지정 (아래 값들 필수)
# DB_HOST=your-rds-endpoint.rds.amazonaws.com
# DB_PORT=3306
# DB_NAME=museumdb

# AWS Secrets Manager Secret 이름 (EC2에서만 사용)
SECRET_NAME=hamo/rds/credentials

# AWS Region (기본값: ap-northeast-2 서울)
AWS_REGION=ap-northeast-2

# DB 연결 확인 건너뛰기 (선택 사항)
SKIP_DB_CHECK=false
